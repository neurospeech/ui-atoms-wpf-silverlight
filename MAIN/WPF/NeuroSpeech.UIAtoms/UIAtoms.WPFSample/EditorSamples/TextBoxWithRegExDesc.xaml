<Page x:Class="UIAtoms.WPFSamples.EditorSamples.TextBoxWithRegExDesc"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="600"
	Title="TextBoxWithRegExDesc">

    <Grid>
        <FlowDocumentScrollViewer>
            <FlowDocument>

                <List>
                    <ListItem>
                        <Paragraph>	This control extends the AtomTextBox so all its associated parameters apply here.</Paragraph>
                    </ListItem>
                </List>

                <List>
                    <ListItem>
                        <Paragraph>	The TextBox here will only accept inputs which are in the format of the regular expression as predefined.</Paragraph>
                    </ListItem>
                </List>

                <List>
                    <ListItem>
                        <Paragraph>	In the current sample, the Regular Expression is set to accept only alpha numeric inputs. Therefore, the control will fire an error if “Signup” is clicked with characters other than alpha-numberic in the Login field.</Paragraph>
                    </ListItem>
                </List>

            </FlowDocument>
        </FlowDocumentScrollViewer>
    </Grid>
</Page>
