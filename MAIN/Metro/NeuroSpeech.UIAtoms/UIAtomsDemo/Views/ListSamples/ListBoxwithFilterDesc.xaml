<navigation:Page x:Class="UIAtomsDemo.Views.ListSamples.ListBoxwithFilterDesc" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
           d:DesignWidth="640" d:DesignHeight="480"
           Title="ListBoxwithFilterDesc Page">
    <Grid x:Name="LayoutRoot">
        <StackPanel>
            <TextBlock Text="•	This is an extension of the ListBox sample displayed above. It has a filter facility linked to the ListBox."
                       TextWrapping="Wrap" FontWeight="Bold"/>
            <TextBlock Text="•	The filter will sort through the data and display only those data items from within the list box that match the string input entered."
                       TextWrapping="Wrap" FontWeight="Bold"/>
            <TextBlock Text="•	The filter can be set to search for data that either starts with the string input, ends with the string input, or contains the string input."
                       TextWrapping="Wrap" FontWeight="Bold"/>
        </StackPanel>

    </Grid>
</navigation:Page>
