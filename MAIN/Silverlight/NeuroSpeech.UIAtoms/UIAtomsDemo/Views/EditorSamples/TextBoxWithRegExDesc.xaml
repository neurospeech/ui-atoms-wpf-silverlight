<navigation:Page x:Class="UIAtomsDemo.Views.EditorSamples.TextBoxWithRegExDesc" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
           d:DesignWidth="640" d:DesignHeight="480"
           Title="TextBoxWithRegExDesc Page">
    <Grid x:Name="LayoutRoot">
        <StackPanel>
            <TextBlock Text="•	This control extends the AtomTextBox so all its associated parameters apply here."
                       TextWrapping="Wrap" FontWeight="Bold"/>
            <TextBlock Text="•	The TextBox here will only accept inputs which are in the format of the regular expression as predefined."
                       TextWrapping="Wrap" FontWeight="Bold"/>
            <TextBlock Text="•	In the current sample, the Regular Expression is set to accept only alpha numeric inputs. Therefore, the control will fire an error if 'Signup' is clicked with characters other than alpha-numberic in the Login field."
                       TextWrapping="Wrap" FontWeight="Bold"/>
        </StackPanel>

    </Grid>
</navigation:Page>
